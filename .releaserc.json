{
  "branches": ["main"],
  "tagFormat": "v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/npm",
      {
        "npmPublish": false
      }
    ],
    [
      "@semantic-release/exec",
      {
        "publishCmd": "npm run build && if [ \"$PUBLISH_ENABLED\" = \"true\" ]; then npm publish --provenance --access public; else echo 'PUBLISH_ENABLED!=true - skipping npm publish (version, tag, and GitHub release still created)'; fi"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": []
      }
    ]
  ]
}
